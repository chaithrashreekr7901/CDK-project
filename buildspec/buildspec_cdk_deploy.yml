version: 0.2

phases:
  install:
    commands:
      - npm install -g aws-cdk
      - pip install -r requirements.txt

  build:
    commands:
      - echo ">>> CDK Deploy Phase for MyMainInfrastructureStack <<<"
      - cdk deploy MyMainInfrastructureStack --require-approval never
