version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
      python: 3.10
    commands:
      - npm install -g aws-cdk
      - pip install -r requirements.txt

  build:
    commands:
      - cdk deploy MyMainInfrastructureStack 

artifacts:
  files:
    - '**/*'
