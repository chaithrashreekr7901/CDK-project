version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt
  build:
    commands:
      - npx cdk synth MyMainInfrastructureStack > main.template.json

artifacts:
  files:
    - main.template.json
